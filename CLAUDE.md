# Claude Entry Point

Before making changes, read and follow `harness/CLAUDE.md`.

Project-specific rules in `harness/` override any global or default agent behavior.

The harness lives in `harness/` so it can sit beside application code such as `src/`, `app/`, or `packages/`.
